# Server Configuration
NODE_ENV=development
# HOST=0.0.0.0 (bind to all interfaces - required for Docker/Kubernetes)
# For local development only, you can use HOST=127.0.0.1 to bind to localhost only
PORT=8721

# Storage Configuration
STORAGE_PATH=./storage

# File Upload Limits
MAX_FILE_SIZE_MB=50
MAX_JSONL_LINES=10000000

# cclogviewer Configuration
# Path to cclogviewer binary (optional - will auto-detect if not set)
# Auto-detection checks:
#   1. /app/bin/cclogviewer (Docker/Kubernetes)
#   2. ~/go/bin/cclogviewer (local development)
# Override only if installed in a custom location
# CCLOGVIEWER_BIN_PATH=/custom/path/to/cclogviewer
CCLOGVIEWER_TIMEOUT_MS=60000

# Rate Limiting
RATE_LIMIT_UPLOAD_WINDOW_MS=900000
RATE_LIMIT_UPLOAD_MAX=10
RATE_LIMIT_VIEW_WINDOW_MS=60000
RATE_LIMIT_VIEW_MAX=100

# Application URL
BASE_URL=http://localhost:8721
